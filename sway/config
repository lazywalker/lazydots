# Default config for sway
#
# Copy this to ~/.config/sway/config and edit it to your liking.
#
# Read `man 5 sway` for a complete reference.

### Variables
#
# Logo key. Use Mod1 for Alt.
set $mod Mod4
# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l
# Your preferred terminal emulator
set $term kitty
# Your preferred application launcher
set $menu wmenu-run

### Output configuration
#
# Default wallpaper (more resolutions are available in /usr/share/backgrounds/sway/)
output * bg /usr/share/backgrounds/sway/Sway_Wallpaper_Blue_1920x1080.png fill
#
# Example configuration:
#
#   output HDMI-A-1 resolution 1920x1080 position 1920,0
#
# You can get the names of your outputs by running: swaymsg -t get_outputs

#exec_always dms run
exec_always noctalia
exec fcitx5 -d
# Dwindle-like auto-alternating split layout
exec_always /home/mic/.config/sway/scripts/dwindle-auto.sh

### Idle configuration
#
# Noctalia's [idle] config relies on ext-idle-notify-v1, which sway 1.12 does
# not implement ("idle notify protocol unavailable; idle behaviors not
# registered"). So under sway we fall back to swayidle via a wrapper script,
# with timeouts matching noctalia/config.toml (lock 300s, screen-off 600s).
# The script is used instead of inline commands because sway's exec mangles
# the quotes around multi-word timeout commands.
exec ~/.config/sway/scripts/idle.sh

### Input configuration
#
# Example configuration:
#
#   input type:touchpad {
#       dwt enabled
#       tap enabled
#       natural_scroll enabled
#       middle_emulation enabled
#   }
#
#   input type:keyboard {
#       xkb_layout "eu"
#   }
#
# You can also configure each device individually.
# Read `man 5 sway-input` for more information about this section.
input "76:617:mic's_Magic_Mouse2" {
    natural_scroll enabled
    pointer_accel -0.4
    accel_profile adaptive
}

### Key bindings
# All keybinds are defined in binds.conf (ported from niri/config.kdl)
include binds

### Window styles
# All window styling is defined in styles (ported from niri/config.kdl)
include styles

#
# Status Bar:
#
# Read `man 5 sway-bar` for more information about this section.
# bar {
#     position top
#
#     # When the status_command prints a new line to stdout, swaybar updates.
#     # The default just shows the current date and time.
#     status_command while date +'%Y-%m-%d %X'; do sleep 1; done
#
#     colors {
#         statusline #ffffff
#         background #323232
#         inactive_workspace #32323200 #32323200 #5c5c5c
#     }
# }
#
include /etc/sway/config.d/*
include ~/.config/sway/noctalia
